

export function saveUserSettings() {
  return function saveUserSettingsClosure(dispatch, reduxState, logger) {
  }
}

export function restoreUserSettings() {
  return function restoreUserSettingsClosure(dispatch, reduxState, logger) {
  }
}
